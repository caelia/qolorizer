(use qolorizer)
(use irregex)
(use getopt-long)

(define (mk-dest-name pattern filename)
    
  #f)
  
(define (process-file src color-spec #!key (blend-mode 'normal) (alpha 1.0) (dest #f) (dest-pattern #f))
  #f)
  
(define (process-dir src color-spec #!key (blend-mode 'normal) (alpha 1.0) (dest #f) (dest-pattern #f))
  #f)
